from setuptools import setup, find_packages
setup(name="census-sensor",
      version="0.0.1",
      author = "Vimal",
      author_email = "vimalsingh99350@gamil.com",
      packages = find_packages(),
      install_requires =['pandas','numpy','flask']
      )
